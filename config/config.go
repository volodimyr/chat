package config

const (
	//Network defines type of network, chat (server, client) is (are) going to use
	Network = "tcp"
	//Port defines port where server runs and client connects
	Port    = ":8080"
)
